# Tasks: Monorepo Setup

- [x] 1.1 Create `pnpm-workspace.yaml` with packages glob and esbuild build allowance
- [x] 1.2 Create root `package.json` with workspace scripts
- [x] 1.3 Create `tsconfig.base.json` with strict TypeScript settings
- [x] 1.4 Create `harness.config.json` with default configuration values
- [x] 1.5 Create `CLAUDE.md` with codebase instructions
- [x] 1.6 Create `AGENTS.md` with mandatory OpenSpec workflow rules
- [x] 1.7 Create `openspec/changes/`, `openspec/changes/archive/`, and `openspec/specs/` with `.gitkeep`
- [x] 1.8 Run `pnpm install` and confirm workspace resolves all packages
